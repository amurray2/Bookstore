@{
    ViewData["Title"] = "Home Page";
}
@model BookListViewModel

@*The table contains some bootstrap. For example when the user hovers over a record the row will be highlighted.*@
<!-- Page Content -->
    <div class="container">

        @*I would like to keep my table view commented instead of deleted so I can reference it for other things*@

        @*<div class="container">
                <table class="table table-hover table-bordered">
                    <thead style="background-color:lightblue;">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Title</th>
                            <th scope="col">Author Name</th>
                            <th scope="col">Publisher</th>
                            <th scope="col">ISBN</th>
                            <th scope="col">Classification</th>
                            <th scope="col">Category</th>
                            <th scope="col">Price</th>
                            <th scope="col">Page Count</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var b in Model.Books)
                        {
                            <tr href="@b.BookId">
                                <th scope="row">@b.BookId</th>
                                <td>@b.Title</td>
                                <td>@b.AuthFirstName @b.AuthMiddleName @b.AuthLastName</td>
                                <td>@b.Publisher</td>
                                <td>@b.ISBN</td>
                                @if (b.Fiction)
                                {
                                    <td>Fiction</td>
                                }
                                else
                                {
                                    <td>Non-Fiction</td>
                                }
                                <td>@b.Category</td>
                                <td>$@b.Price</td>
                                <td>@b.NumberPages</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>*@
        <div class="container">

            @foreach (var b in Model.Books)
            {
                <div class="row justify-content-center" id="cardDiv-@b.BookId" onmouseover="myFunction(@b.BookId)" onmouseout="myFunction2(@b.BookId)">
                    <partial name="BookSummary" model="b" />

                </div>
                <hr />
                <br />
            }
        </div>
        <br />
        <div class="container">
            <div class="row justify-content-start">
                <div class="col-6" style="text-align:center">
                    <div page-model="@Model.PagingInfo" page-action="Index" page-classes-enabled="true"
                         page-class="btn" page-class-normal="btn-outline-dark" page-class-selected="btn-primary"
                         page-url-category="@Model.CurrentCategory" class="btn-group pull-right m-1"></div>
                </div>
            </div>
        </div>
        <br />
    </div>